# FS-platform: Platform-wide behavior

The root project pulls together its sub-projects: the API in §api/FS-session
and the web app in §web/FS-session.

Local citation back to this same file: §FS-platform.
