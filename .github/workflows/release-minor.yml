name: Release minor

# Manual minor release helper: from the latest vX.Y.Z tag, bump to vX.(Y+1).0,
# commit the workspace version update to main, and dispatch release.yml to
# publish crates + binaries. release.yml remains the publishing workflow; this
# file only prepares the version bump.

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write   # gh workflow run needs actions:write to dispatch

env:
  CARGO_TERM_COLOR: always

jobs:
  minor-release:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # Use the same release PAT as auto-bump.yml so the version commit can
          # pass the main-branch ruleset bypass configured for releases.
          token: ${{ secrets.RELEASE_PAT }}

      - name: Resolve current and next versions
        id: versions
        shell: bash
        run: |
          set -euo pipefail
          tag="$(git tag --list 'v*.*.*' --sort=-v:refname | head -n1)"
          if [ -z "$tag" ]; then
            echo "error: no v*.*.* tag exists; bootstrap the first release manually" >&2
            exit 1
          fi
          current="${tag#v}"
          IFS=. read -r major minor patch <<< "$current"
          next="${major}.$((minor + 1)).0"
          echo "current=$current"  >> "$GITHUB_OUTPUT"
          echo "next=$next"        >> "$GITHUB_OUTPUT"
          echo "current_tag=$tag"  >> "$GITHUB_OUTPUT"
          echo "next_tag=v$next"   >> "$GITHUB_OUTPUT"
          echo "Latest tag: $tag; proposed minor release: v$next"

      - name: Gate - commits since last tag
        shell: bash
        run: |
          set -euo pipefail
          tag="${{ steps.versions.outputs.current_tag }}"
          count="$(git rev-list --count "$tag"..HEAD)"
          if [ "$count" -eq 0 ]; then
            echo "error: no commits since $tag; refusing to create an empty minor release" >&2
            exit 1
          fi
          echo "$count commits since $tag."

      - name: Gate - CI on current main tip is green
        shell: bash
        run: |
          set -euo pipefail
          sha="$(git rev-parse HEAD)"
          # The CI workflow runs on push to main. Find the most recent run that
          # targeted this exact tip; if none exists yet, the bump must wait.
          run="$(gh run list --workflow=CI --branch=main --limit=20 \
            --json status,conclusion,headSha,databaseId \
            | jq -c ".[] | select(.headSha == \"$sha\")" | head -n1)"

          if [ -z "$run" ]; then
            echo "error: no CI run found for HEAD ($sha); rerun once CI has finished" >&2
            exit 1
          fi
          status="$(echo "$run" | jq -r '.status')"
          conclusion="$(echo "$run" | jq -r '.conclusion')"
          if [ "$status" != "completed" ] || [ "$conclusion" != "success" ]; then
            echo "error: CI on $sha is $status/$conclusion (need completed/success)" >&2
            exit 1
          fi
          echo "CI on $sha is green."

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-edit
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-edit

      - name: Bump workspace version and refresh lockfile
        shell: bash
        run: |
          set -euo pipefail
          cargo set-version --workspace "${{ steps.versions.outputs.next }}"
          cargo update --workspace
          printf 'grund %s\n' "${{ steps.versions.outputs.next }}" > e2e/cases/cli-version/expected.stdout

      - name: Commit and push bump
        shell: bash
        run: |
          set -euo pipefail
          next="${{ steps.versions.outputs.next }}"

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'

          git add Cargo.toml crates/grund-core/Cargo.toml Cargo.lock e2e/cases/cli-version/expected.stdout
          git commit -m "Release v${next}"
          git push origin main

      - name: Dispatch release workflow
        shell: bash
        run: |
          set -euo pipefail
          next="${{ steps.versions.outputs.next }}"
          # GITHUB_TOKEN pushes do not fan out to other workflows, so dispatch
          # release.yml directly from main after the version bump commit lands.
          gh workflow run release.yml \
            --ref main \
            -f version="$next" \
            -f publish_crates=true \
            -f create_github_release=true
          echo "Dispatched release.yml for v$next."
