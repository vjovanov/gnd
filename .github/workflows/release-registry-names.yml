name: Release registry names

on:
  workflow_dispatch:

jobs:
  registry-names:
    name: verify package-name availability
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check package names
        run: bash scripts/check-registry-names.sh
